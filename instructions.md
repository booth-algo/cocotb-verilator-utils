# Instructions (Temporary)

To test out its functionality, go to `cocotb-yaml-runner/` and run `pip install -e .` to install the pip package in active development mode.
Go back to `/`.
Run `cocotb-yaml --help` to explore options.
For example, `cocotb-yaml --test test_example_using_interface`.

The .gitignore is very incomplete.
